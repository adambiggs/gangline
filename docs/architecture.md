# Architecture

The current Go runtime, package boundaries, event/effect loop, state layout,
collar path, and verified-delivery protocol are documented in the repository's
[architecture overview](../ARCHITECTURE.md).
