# Gangline

A harness harness: drive a team of CLI coding agents as tmux windows with
verified, identity-prefixed messaging.

This PyPI package reserves the name. The tool ships via npm (`npm i -g gangline`)
and from source. See <https://gangline.ai>
