packaging/pypi/dist/
packaging/pypi/build/
packaging/pypi/*.egg-info/
*.tgz
.gang-briefs/
.claude/settings.local.json
.pii-scan-denylist
# The default install directory is also the data directory; the usage ledger
# gang writes there is not a local change to the installed tree.
/usage/
